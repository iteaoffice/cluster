{% do headTitle().append(translate("txt-admin")) %}
{% do headTitle().append(translate("txt-edit-organisation-type")) %}

<h1>{{ translate("txt-edit-cluster") }}</h1>

{% do form.prepare() %}
{{ form().openTag(form)|raw }}

{% set clusterFieldset = form.get('cluster_entity_cluster') %}

{{ ztbformelement(clusterFieldset.get('id')) }}
{{ ztbformelement(clusterFieldset.get('name')) }}
{{ ztbformelement(clusterFieldset.get('description')) }}

{% if cluster.hasLogo() %}
    <div class="form-group row">
        <label class="col-sm-3 col-form-label">{{ translate("txt-current-logo") }}</label>

        <div class="col-sm-9">
            <p class="form-control-plaintext">{{ clusterLogo(cluster, 300) }}</p>
        </div>
    </div>
{% endif %}

{{ ztbformelement(form.get('file')) }}
{{ formelement(form.get('csrf')) }}

<div class="form-group row">
    <div class="offset-sm-3 col-sm-9">
        {{ ztbformelement(form.get('submit')) }}
        {% if form.has('delete') %}
            {{ ztbformelement(form.get('delete')) }}
        {% endif %}
        {{ ztbformelement(form.get('cancel')) }}
    </div>
</div>

{{ form().closeTag()|raw }}

